{
  "name": "qodo-agent-team-gui",
  "version": "1.0.0",
  "description": "Web-based GUI for orchestrating Qodo Agent Team scripts",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n server,vite -c blue,magenta \"node server.js\" \"cd frontend && npm run dev\"",
    "start": "node server.js",
    "server": "node server.js",
    "build": "cd frontend && npm run build"
  },
  "dependencies": {
    "chokidar": "^3.6.0",
    "express": "^4.21.0",
    "socket.io": "^4.7.5",
    "tree-kill": "^1.2.2",
    "uuid": "^10.0.0"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
